[project]
name = "ai-interview"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Flask==2.3.3",
    "Werkzeug==2.3.7",
    "upstash-redis==1.4.0",
]

[tool.vercel]
entrypoint = "web_app:app"
